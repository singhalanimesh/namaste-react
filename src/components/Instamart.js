const Instamart = () => {
  return <h1>Grocery has a lot of Subcomponents, so we use Lazy Loading to only load it when necessary so as to improve the performance of our app</h1>;
};

export default Instamart;